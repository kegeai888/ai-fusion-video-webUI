-- ==========================================================
-- V3: 分镜条目新增视频提示词字段
-- ==========================================================

ALTER TABLE afv_storyboard_item ADD COLUMN video_prompt TEXT NULL COMMENT 'AI生成视频时使用的提示词（保存以便复用和手动调整）';
